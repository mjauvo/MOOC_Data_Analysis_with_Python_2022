#!/usr/bin/env python3

import numpy as np

# Gets a two dimensional array 'a' and an index 'c' of a column
# as parameters, and returns an array whose rows are sorted based
# on column c, in the following way:
# 
# Rows are ordered so that those rows with the most frequent element
# in column c come first, then come the rows with the second most
# frequent element in column c, and so on.
def most_frequent_first(a, c):
    return np.array([])

def main():
    pass

if __name__ == "__main__":
    main()
